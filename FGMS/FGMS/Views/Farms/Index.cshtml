@model IEnumerable<FGMS.Models.Farm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FarmName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.acrage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.scrapieFlockId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FarmName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.acrage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.scrapieFlockId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.farmId }) |
            @Html.ActionLink("Details", "Details", new { id=item.farmId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.farmId })
        </td>
    </tr>
}

</table>
